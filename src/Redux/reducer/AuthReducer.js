
const initval = {
    isLoading : false,
    user: null,
    error: ''
}

export const AuthReducer = (state = initval, action) => {
    switch (action.type) {
        default:
            return state
    }
}